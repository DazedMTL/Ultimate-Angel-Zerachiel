/*:
 * @target MV
 */
(() => {
  "use strict";

  // 敗北時メッセージの削除
  BattleManager.displayDefeatMessage = function () {
    //        $gameMessage.add(TextManager.defeat.format($gameParty.name()));
  };
})();
